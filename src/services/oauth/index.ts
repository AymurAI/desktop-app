import getNewToken from './getNewToken';
import onceTokenReceived from './onceTokenReceived';
import openConsentScreen from './openConsentScreen';

const oauth = {
  getNewToken,
  onceTokenReceived,
  openConsentScreen,
};

export default oauth;
